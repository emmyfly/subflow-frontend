# Shared secret sent as the X-Admin-Token header on admin-only backend requests
# (tenants, subscribers, payments, dunning queue, webhooks lists).
# Get the real value from whoever manages the backend, then set it here for
# local dev and in the Vercel project's Environment Variables for production.
VITE_ADMIN_TOKEN=
